/** Tailwind CSS v4 uses the PostCSS plugin; no tailwind.config.js (CSS-first config lives in app/globals.css). */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
